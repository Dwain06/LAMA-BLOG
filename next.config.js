/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.pexels.com",
      "images.unsplash.com",
      "fakestoreapi.com",
      "xsgames.co",
      "randomuser.me",
    ],
  }
}

module.exports = nextConfig
